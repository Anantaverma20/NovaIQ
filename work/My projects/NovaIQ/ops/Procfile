# Procfile for Render/Railway deployment
web: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
# worker: cd backend && celery -A app.tasks.schedule worker --loglevel=info
# beat: cd backend && celery -A app.tasks.schedule beat --loglevel=info

